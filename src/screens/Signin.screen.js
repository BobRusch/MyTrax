import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

export default SigninScreen = () => {
  return <Text style={{ fontSize: 48 }}>Singin Screen</Text>;
};

const styles = StyleSheet.create({});
